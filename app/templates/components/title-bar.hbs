<div class="titlebar">
  <div class="team-project-wrapper">
    <img class="team-img" src="{{dataService.team.image}}">
    <div class="titlebar-team-name">
      {{dataService.team.name}}
    </div>
	  <div class="vr"></div>
    <div class="titlebar-project-name">
      {{dataService.group.name}}
    </div>
  </div>
  <div class="connection-wrapper">
    <div class="connection-status">
      {{#if isOnline}}
        {{ui-icon 'circle' size='12' color='#27ae60'}}
        Connected
      {{else}}
        {{ui-icon 'circle' size='12' color='#c0392b'}}
        Reconnecting...
      {{/if}}
    </div>
  </div>
  <div class="current-branch-wrapper">
    <div class="current-branch">
      {{ui-icon 'github'}}
      {{dataService.status.currentBranch}}
    </div>
  </div>
  <div class="current-user-wrapper">
    {{user-dropdown logout='logout'}}
  </div>
</div>
